# SDD：Mac 截圖與圖片編輯工具
**版本：** 0.1（MVP 草稿）
**日期：** 2026-03-21
**狀態：** 待審閱

---

## 1. 專案概述

### 1.1 背景
Mac 生態系缺乏能同時涵蓋「截圖、標註編輯、疊圖、格式轉換」的一體化工具。現有工具（Shottr、Snagit 等）各有缺口，無法對標 Windows 上的 FastStone Capture 使用體驗。

### 1.2 目標使用者
需要在 Mac 上進行多模態工作溝通的非技術使用者（PM、設計師、跨部門溝通人員）。

### 1.3 核心原則
- **MVP 優先**：只做確認的六個功能模組，其餘列入後續迭代
- **操作直覺**：使用者不需要閱讀說明書
- **輸出即用**：產出的圖片可直接貼入 Google Docs、Slack、Email

---

## 2. 技術棧

| 層級 | 技術 | 用途 |
|------|------|------|
| 應用框架 | Electron | 跨平台桌面應用殼層 |
| 前端 UI | HTML / CSS / JavaScript | 介面渲染 |
| 圖片處理 | Sharp (Node.js) | 格式轉換、縮放、裁切、合成 |
| 網頁截圖 | Playwright (Chromium) | URL 完整頁面截圖 |
| 桌面截圖 | Electron desktopCapturer + Screenshot API | 全螢幕、視窗、區域截圖 |
| 標註渲染 | HTML5 Canvas | 標註層繪製與燒錄 |

---

## 3. 功能規格

### S1｜桌面截圖

**觸發方式：**
- 工具列按鈕
- 全域快捷鍵（可設定）

**截圖模式：**

| 模式 | 行為 |
|------|------|
| 全螢幕 | 立即截取整個螢幕 |
| 當前視窗 | 點選任一視窗後截取該視窗範圍 |
| 矩形選取 | 拖拉繪製矩形範圍後截取 |

**輸出：**
- 截圖結果自動進入**編輯畫面（E1–E3）**
- 同時複製到系統剪貼簿

**邊界條件：**
- 多螢幕環境：截取游標所在螢幕
- 取消選取（按 Esc）：回到主畫面，不產生輸出

---

### S2｜網頁截圖（完整頁面）

**觸發方式：** 主畫面「網頁截圖」按鈕

**操作流程：**
1. 使用者在輸入框貼上或輸入 URL
2. 按下「截圖」按鈕
3. App 顯示載入中狀態（進度條）
4. Playwright 啟動 headless Chromium，載入頁面並等待完成
5. 截取完整頁面（含滾動區域）
6. 結果進入編輯畫面

**輸入規格：**
- 接受 `http://` 和 `https://` 開頭的 URL
- 無效 URL 顯示錯誤提示，不執行截圖

**輸出規格：**
- 圖片寬度：1440px（桌面標準視窗寬，可設定）
- 圖片高度：依頁面實際高度
- 預設格式：PNG

**限制：**
- 需要登入才能檢視的頁面：截到登入牆為止，不另行處理
- 動態載入內容（無限滾動）：截取初始載入完成的畫面

---

### E1｜圖片尺寸調整

**觸發方式：** 編輯畫面工具列「調整大小」

#### E1-A 等比例縮放
- 使用者輸入目標寬度（px）
- 高度自動依原始比例計算並顯示（唯讀）
- 使用者確認後套用

#### E1-B 裁切
- 使用者在畫面上拖拉選取矩形區域
- 選取框顯示目前尺寸（px）
- 確認後裁切，其餘區域丟棄
- 可重新選取（確認前可取消）

**邊界條件：**
- 縮放最小值：10px（寬或高）
- 縮放最大值：原始尺寸的 400%
- 裁切區域不可為 0px

---

### E2｜圖片標註

**觸發方式：** 編輯畫面工具列各標註工具按鈕

#### 支援的標註元素

| 元素 | 操作 | 說明 |
|------|------|------|
| 矩形框 | 拖拉繪製 | 中空框線，無填色 |
| 箭頭 | 拖拉起終點 | 單向箭頭，終點帶箭頭頭 |
| 文字 | 點選位置後輸入 | 自由文字，可設定字型大小 |
| 編號標記 | 點選位置放置 | 圓形底色 + 數字，自動遞增（1, 2, 3…） |

#### 共用屬性（每個元素獨立設定）
- **顏色**：色盤選色（預設提供 8 色：紅、藍、綠、黃、橙、紫、黑、白）
- **線條粗細**（框、箭頭）：細 / 中 / 粗

#### 編輯層行為
- **編輯中**：每個標註元素為獨立物件，可點選選取、拖動移動、刪除（Delete 鍵）
- **確認燒錄**：按「完成」按鈕後，所有標註合併進底圖，不可再拆開
- **燒錄前可撤銷**：Cmd+Z 逐步撤銷標註動作

**邊界條件：**
- 最多標註元素數量：無硬性限制（效能考量，實際測試後決定是否加上警示）
- 編號標記：手動刪除後，編號不自動重新排序（保持語意一致性）

---

### E3｜疊圖

**觸發方式：** 編輯畫面工具列「疊入圖片」

**操作流程：**
1. 使用者點選「疊入圖片」，開啟系統檔案選取視窗
2. 選取圖片後，疊入圖出現在畫布中央
3. 使用者可：
   - **拖動**：移動疊入圖的位置
   - **拖拉邊角**：等比例縮放疊入圖大小
4. 點選其他工具或按「完成」後，疊入圖燒錄進底圖

**支援疊入格式：** JPG、PNG、WebP、GIF（靜態，取第一幀）、SVG

**視覺回饋：**
- 疊入圖選取中：顯示邊框 + 8 個控制點（四角 + 四邊中點）
- 等比例縮放鎖定：拖角時保持長寬比

**邊界條件：**
- 疊入圖可超出底圖邊界（超出部分在燒錄時裁切）
- 一次只能操作一張疊入圖（下一張疊入前需先燒錄或取消）

---

### F1｜格式轉換

**觸發方式：**
1. 儲存時選擇目標格式（主要路徑）
2. 獨立的「批次轉換」功能（次要路徑，詳見下方）

#### F1-A 儲存時轉換

使用者在「另存新檔」時，從格式下拉選單選取目標格式：

| 格式 | 說明 |
|------|------|
| PNG | 無損，支援透明背景（Alpha 通道） |
| JPG | 有損壓縮，可設定品質（預設 90%） |
| WebP | 現代格式，較小檔案，支援透明 |
| GIF | 靜態 GIF，不支援透明 |
| SVG → PNG | 特殊單向轉換（見 F1-B） |

#### F1-B SVG → PNG 轉換

**觸發方式：** 主畫面「SVG 轉 PNG」快速按鈕

**操作流程：**
1. 選取 .svg 檔案
2. 設定輸出寬度（高度自動計算）
3. 選擇背景色（透明 / 白色，預設白色）
4. 輸出 PNG

**設計背景：** 專為 Claude 輸出的流程圖 SVG 轉換為可貼入 Google Docs 的 PNG 設計。

#### F1-C 批次轉換
- 選取多個檔案
- 選擇目標格式
- 一鍵轉換，輸出到同目錄（或指定目錄）

---

## 4. UI 流程

```
啟動 App
│
├── 主畫面
│   ├── [桌面截圖] 按鈕 → 選擇模式 → 截圖 → 編輯畫面
│   ├── [網頁截圖] 按鈕 → 輸入 URL → 截圖 → 編輯畫面
│   ├── [開啟圖片] 按鈕 → 選取檔案 → 編輯畫面
│   └── [SVG 轉 PNG] 按鈕 → 選取 SVG → 設定 → 輸出
│
└── 編輯畫面
    ├── 左側工具列：縮放、裁切、矩形框、箭頭、文字、編號、疊圖
    ├── 頂部：顏色選取、線條粗細
    ├── 畫布：圖片 + 標註層
    └── 右下角：[完成並儲存] → 選擇格式 → 輸出檔案
```

---

## 5. 測試案例（TDD 清單）

### S1 桌面截圖
- [ ] 全螢幕截圖產生圖片，尺寸等於螢幕解析度
- [ ] 矩形選取截圖，輸出尺寸等於選取範圍
- [ ] 按 Esc 取消選取，不產生圖片、不進入編輯畫面
- [ ] 截圖結果自動複製到剪貼簿

### S2 網頁截圖
- [ ] 有效 URL 產生完整頁面截圖（高度 > 1 個螢幕高度的頁面）
- [ ] 無效 URL 顯示錯誤訊息，不崩潰
- [ ] 截圖期間顯示載入狀態
- [ ] HTTPS 和 HTTP 網址皆可處理

### E1 尺寸調整
- [ ] 輸入寬度 800px，高度自動計算為正確等比值
- [ ] 裁切後圖片尺寸等於選取框尺寸
- [ ] 縮放至最小值 10px 不崩潰
- [ ] 取消裁切選取，圖片不變

### E2 標註
- [ ] 新增矩形框，顏色為選取的顏色
- [ ] 新增箭頭，終點有箭頭頭
- [ ] 新增文字，內容正確顯示
- [ ] 新增編號，第一個為 1，第二個為 2
- [ ] 拖動標註元素，位置正確更新
- [ ] Cmd+Z 撤銷最後一個標註
- [ ] 按「完成」後標註燒錄，無法再選取標註元素
- [ ] 可同時存在不同顏色的框（藍框 + 紅框）

### E3 疊圖
- [ ] 疊入 PNG 圖片，顯示在畫布中央
- [ ] 拖動疊入圖，位置正確更新
- [ ] 拖拉邊角縮放，長寬比保持不變
- [ ] 疊入 SVG，正確渲染（不顯示為空白）
- [ ] 燒錄後疊入圖合併進底圖

### F1 格式轉換
- [ ] PNG 存檔，透明背景（Alpha 通道）保留
- [ ] JPG 存檔，檔案大小小於原始 PNG
- [ ] WebP 存檔，檔案正確開啟
- [ ] SVG → PNG 轉換，輸出尺寸符合設定的寬度
- [ ] SVG → PNG 轉換，白色背景選項正確套用
- [ ] 批次轉換 3 個檔案，全部輸出成功

---

## 6. 後續迭代（Out of Scope for MVP）

| 功能 | 說明 |
|------|------|
| 並排拼版 | 多圖上下/左右排列成一張 |
| 自由筆選區截圖 | 不規則形狀截圖 |
| 固定區域截圖 | 記憶座標的重複截圖 |
| 動態 GIF 製作 | 多幀合成動畫 |
| PNG/JPG → SVG | 向量追蹤轉換 |
| 截圖後直接傳送 | 寄信、傳 Slack 等 |
| 標註樣式預設集 | 儲存常用顏色/樣式組合 |

---

## 7. 開放問題（待決定）

| # | 問題 | 影響範圍 |
|---|------|---------|
| Q1 | App 分發方式：直接下載 .dmg 或上 Mac App Store？ | 影響部分 API 權限（截圖 API 需要系統授權） |
| Q2 | 是否需要「歷史截圖」紀錄功能？ | UI 複雜度 |
| Q3 | 全域快捷鍵預設值為何？ | UX 設計 |

---

*本文件為 SDD 第一版草稿，請審閱後標記需修改的項目。*
